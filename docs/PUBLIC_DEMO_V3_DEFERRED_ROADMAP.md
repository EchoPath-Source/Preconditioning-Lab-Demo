# Public Demo v3 — Deferred Roadmap

Demo v3 is preserved as the future **Kernel Bridge Replay Console / Spatial Decision Debugger prototype**.

```text
v0 = static visual diagnostic viewer
v1 = local loader + public-safe stress fixtures
v2 = scene/bridge/Harp correlation — complete
v3 = kernel-ready bridge replay console — deferred
```

V3 is intentionally not required before the E-Tree Translation and CASE Library bootstrap.

## Future public-safe chain

```text
private kernel facade
-> explicit public-safe replay projection
-> bridge/Harp/optional closure-Hodge artifacts
-> browser replay console
```

## Planned inputs

```text
spec005_culling_scene_v0 JSON
future qrrg_kernel_bridge_replay_v0 JSON
qrrg_preconditioning_bridge_event_v0 JSONL
optional qrrg_harp_repair_event_v0 JSONL
optional qrrg_closure_hodge_diagnostic_v0 JSONL
```

## Planned panels

```text
route status
anchor/component connectivity
ordered bridge replay timeline
Harp residual class
optional closure/Hodge enrichment
scene/address correlation
public-safe JSON/Markdown export
```

## Required blocker presentation

```text
anchors admissible
start_goal_same_component = false
connectivity_stage = graph_connectivity
no_path_reason = start_goal_disconnected_components
```

## Non-goals

```text
no live private-kernel call
no backend upload
no raw private diagnostics
no Don source files
no product validation or public SDK claim
no Hodge/physics claim
no dependency on private E-Tree internals
```

The canonical detailed specification is maintained in the private Preconditioning Lab at:

```text
docs/updates/PUBLIC_DEMO_V3_KERNEL_BRIDGE_REPLAY_SPEC.md
```
